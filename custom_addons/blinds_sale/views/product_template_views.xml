<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <record id="view_product_template_form_inherit" model="ir.ui.view">
            <field name="name">product.template.form.inherit</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='standard_price']" position="attributes">
                    <attribute name="groups">purchase.group_purchase_user,purchase.group_purchase_manager</attribute>
                </xpath>

                <xpath expr="//field[@name='name']" position="after">
                    <field name="formula_id" placeholder="Choose formula"/>
                </xpath>
                
                <xpath expr="//field[@name='categ_id']" position="after">
                    <field name="height"/>
                    <field name="width"/>
                    <field name="profit_percentage"/>
                </xpath>

                <xpath expr="//notebook" position="inside">
                    <page string="Optional Settings">
                        <group>
                            <field name="is_auto_add"/>
                            <field name="is_auto_hide"/>
                            <field name="use_parent_dimensions"/>
                            <field name="is_kpi_product"/>
                            <field name="kpi_employee" invisible="not is_kpi_product" />
                            <field name="product_qty_field" />
                        </group>
                    </page>
                </xpath>
            </field>
        </record>
        
        <record id="view_product_category_form_inherit" model="ir.ui.view">
            <field name="name">product.category.form.inherit</field>
            <field name="model">product.category</field>
            <field name="inherit_id" ref="product.product_category_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='parent_id']" position="after">
                    <field name="image_1920" widget="image"/>
                    <field name="formula_id" />
                    <field name="price_formula_id"/>
                    <field name="hide_from_mobile_app"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>